{
  "name": "media-service",
  "version": "1.0.0",
  "description": "RTMP ingest + HLS transcoding service for OmexLive, split out of the main API so a heavy stream's ffmpeg transcoding load never blocks REST API latency.",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "node --test src/**/*.test.js"
  },
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.700.0",
    "chokidar": "^3.6.0",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "mongoose": "^9.4.1",
    "node-media-server": "^4.2.4"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
